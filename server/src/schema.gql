# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Mutation {
  createTweet(text: String!): Tweet!
  login(password: String!, username: String!): User!
  me: User!
  signup(password: String!, username: String!): User!
}

type Query {
  feed(userId: String!): [Tweet!]!
  follow(followeeId: String!, followerId: String!): Boolean!
  getProfileFeed(userId: String!): [Tweet!]!
  getTweetById(tweetId: String!): [Tweet!]!
  unfollow(followeeId: String!, followerId: String!): Boolean!
}

type Tweet {
  createdAt: DateTime!

  """
  The `ID` scalar type represents a unique identifier, often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"4"`) or integer (such as `4`) input value will be accepted as an ID.
  """
  ID: Float!
  text: String!
  user: User
}

type User {
  """
  The `ID` scalar type represents a unique identifier, often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"4"`) or integer (such as `4`) input value will be accepted as an ID.
  """
  ID: String!
  username: String!
}
